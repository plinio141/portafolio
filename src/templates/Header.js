const Header = () => {
    const view = `
        <div class="header-nav">
            <a href="#projects" class="nav-item">
                PROYECTOS
            </a>
            <a href="#about" class="nav-item">
                CONTACTO
            </a>
        </div>
    `;

    return view;
}

export default Header;